# Чеклист деплоя b5-agent на продакшен

## Перед деплоем

- [ ] Убедитесь, что в `.env.production` указаны правильные URL:
  ```
  VITE_API_BASE_URL=https://api.bonus.band
  VITE_AUTH_API_URL=https://auth.bonus5.ru
  VITE_FRONTEND_URL=https://bonus5.ru
  NODE_ENV=production
  ```

- [ ] Проверьте, что в коде используется правильный домен для куки (`.bonus5.ru`)

## Деплой

1. Соберите production build:
   ```bash
   npm run build
   ```

2. Запустите на сервере:
   ```bash
   NODE_ENV=production node build
   ```

## После деплоя - проверка

### 1. Проверка куки после логина

1. Откройте https://bonus5.ru/login
2. Войдите в систему
3. Откройте DevTools → Application → Cookies
4. Найдите куку `b5_auth_token` и проверьте:
   - ✅ Domain: `.bonus5.ru`
   - ✅ Path: `/`
   - ✅ HttpOnly: true
   - ✅ Secure: true
   - ✅ SameSite: Lax

### 2. Проверка SSR на странице финансов

1. Откройте https://bonus5.ru/finances
2. Откройте DevTools → Network
3. Обновите страницу (F5)
4. Найдите запрос к `/finances`
5. Проверьте:
   - ✅ В Request Headers должна быть кука `b5_auth_token`
   - ✅ В Response должен быть HTML с данными (не пустой скелетон)
   - ✅ Данные отображаются сразу без дополнительных API запросов

### 3. Проверка логов сервера

Проверьте логи сервера на наличие:

```
✅ Успешная аутентификация:
🔐 Auth middleware: User authenticated { email: '...', path: '/finances' }
💰 Finances SSR: Starting data load
✅ Finances SSR: Loaded data in XXXms

❌ Проблемы с аутентификацией:
⚠️ Auth middleware: No token found in httpOnly cookie
⚠️ Finances SSR: No authentication token found in httpOnly cookie
```

### 4. Проверка logout

1. Нажмите "Выйти"
2. Проверьте в DevTools → Application → Cookies
3. Убедитесь, что куки `b5_auth_token` и `b5_auth_user` удалены

## Возможные проблемы

### Проблема: Кука не устанавливается

**Причина:** Неправильный домен или настройки безопасности

**Решение:**
- Проверьте, что `NODE_ENV=production`
- Проверьте, что сайт работает по HTTPS
- Проверьте настройки CORS на бэкенде

### Проблема: Данные не загружаются на странице финансов

**Причина:** Кука не передаётся при SSR запросах

**Решение:**
- Проверьте логи сервера на наличие токена в `hooks.server.js`
- Убедитесь, что домен куки правильный (`.bonus5.ru`)
- Проверьте, что кука не истекла (maxAge)

### Проблема: Кука не удаляется при logout

**Причина:** Domain не совпадает при удалении

**Решение:**
- Убедитесь, что в `logout-jwt/+server.js` используется тот же домен, что и при установке

## Откат изменений

Если что-то пошло не так, откатите изменения в файлах:
- `b5-agent/src/routes/api/auth/login-jwt/+server.js`
- `b5-agent/src/routes/api/auth/logout-jwt/+server.js`
- `b5-agent/src/hooks.server.js`

И пересоберите проект.
